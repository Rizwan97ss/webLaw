---
import { MoveRight } from 'lucide-react';
const {
  href = "#",
  label = "Get Started",
  variant = "primary",
} = Astro.props;
---

<a href={href} class={`btn btn--${variant}`}>
  {label}
  <MoveRight size={16} />
</a>